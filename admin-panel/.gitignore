# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.DS_Store
dist
dist-ssr
*.local

/cypress/videos/
/cypress/screenshots/

# 👉 Custom Git ignores

# Editor directories and files
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
!.vscode/*.code-snippets
!.vscode/tours
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.yarn

# iconify dist files
# NOT: build:icons scripti bu ortamda (Node ESM) çalışmadığından icons.css
# placeholder olarak repoda tutuluyor; aksi halde dev server import hatası verir.
# src/plugins/iconify/icons.css

# Ignore MSW script
public/mockServiceWorker.js

# Env files
# .env.development repoda tutuluyor (sadece localhost API URL'i içerir, gizli değil).
# Gizli env değerleri .env.local veya .env.production.local içinde olmalı (bunlar ignore'da).
.env.local
.env.*.local
